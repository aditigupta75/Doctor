# This portfolio is a static site and needs no environment variables to deploy.
#
# Keep future public configuration values in this file as documentation only.
# Never store passwords, tokens, or private keys in client-side HTML/CSS/JavaScript.
#
# Example (only if the site is later converted to use a build tool):
# PUBLIC_SITE_URL=https://your-domain.vercel.app
